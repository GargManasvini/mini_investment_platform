# This is an example environment file.
# To run this project, make a copy of this file and name it ".env".
# Then, fill in your own local values for the variables below.

# --- MySQL Database Credentials ---
# The root password for your MySQL instance.
DB_ROOT_PASSWORD=your_mysql_root_password
# The name of the database the application will use.
DB_DATABASE=gripInvest
# The user for the application to connect with.
DB_USER=root
# The password for the application user.
DB_PASSWORD=your_mysql_password

# --- Backend API Configuration ---
# A long, random, and secret string used for signing JSON Web Tokens.
JWT_SECRET=generate_a_strong_and_random_secret_key
# A comma-separated list of emails that should have admin privileges.
ADMINS=your_admin_email@example.com
