# Handle all text files automatically
* text=auto eol=lf

# Ensure Docker-related files always use LF
*.yml text eol=lf
*.yaml text eol=lf
Dockerfile text eol=lf

# Ensure shell scripts use LF (important for Linux containers)
*.sh text eol=lf

# Ensure Node.js files use LF
*.js text eol=lf
*.ts text eol=lf
*.json text eol=lf
*.env text eol=lf

# Ensure frontend files use LF
*.html text eol=lf
*.css text eol=lf

# Keep Windows batch files with CRLF (only when needed)
*.bat text eol=crlf
*.cmd text eol=crlf
